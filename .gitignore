# --- raw data and anything derived from it -------------------------------
# The SKEMPI distribution is redistributable but large; the README says how to fetch it.
data/skempi_v2.csv
data/SKEMPI2_PDBs.tgz
data/PDBs/
data/processed/
data/features/
# folds.csv is NOT ignored: the frozen split is a committed artefact (see src/splits.py)

# --- generated results ---------------------------------------------------
reports/**
!reports/.gitkeep
# figures are deliverables the README points at, so they are versioned
!reports/figures/
!reports/figures/**

# --- third-party model weights and checkouts -----------------------------
third_party/
*.pt
*.pth

# --- python --------------------------------------------------------------
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/

# --- misc ----------------------------------------------------------------
# AppleDouble resource forks shipped inside the SKEMPI structure tarball
._*
.DS_Store

# The assignment brief is Converge's document, not part of the deliverable. Keeping it out of
# the repo avoids republishing their interview material if this is ever made public.
Converge Bio - Machine Learning Researcher - Home Assignment - External.pdf

# Claude Code tooling. Vendored skill packages are environment setup, not project code.
# A project-level .claude/settings.json would be worth committing; the skills bundle is not.
.claude/skills/

# live dashboard pulls these off a running VM; transient
scripts/dashboard/_data/

# GPU run artefacts: 20 MB tarballs pulled off the VM, and their extractions.
# The runs that matter are unpacked into reports/ and tracked there.
reports_colab/

# --- overnight fusion run -------------------------------------------------
# results/ is otherwise ignored; these are the morning deliverables.
!results/results.csv
!results/benchmarks.csv
!results/summary.md
!results/error_analysis.md
!results/failures.md
!results/predictions/
!results/predictions/**
